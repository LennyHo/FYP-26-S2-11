/*function isChatbotAddToCartRequest(message) {
  const msg = String(message || "").toLowerCase();

  return (
    /add.*cart/.test(msg) ||
    /put.*cart/.test(msg) ||
    /order.*this/.test(msg) ||
    /i want.*(milk tea|tea|latte|drink)/.test(msg)
  );
}

module.exports = {
  isChatbotAddToCartRequest,
};
*/